# Core deps to reproduce the final result (scripts/reproduce_final.py):
#   numpy, pandas, scikit-learn  — everything below those is only for the full retrain.
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.4
lightgbm>=4.0
xgboost>=2.0
catboost>=1.2
# Time-series specific feature libraries (Tier-1 expansion)
# pycatch22 needs a C compiler (no prebuilt wheel on some platforms) and is OPTIONAL:
# the feature code guards its absence (have_c22) and reproduce_final.py does not use it.
# Enable for full feature parity when retraining:  pip install pycatch22
# pycatch22>=0.4
sktime>=0.30      # for MiniRocket
# tsfresh is heavy and slow — install on demand if EfficientFCParameters subset is needed
# tsfresh>=0.20
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66
pyarrow>=15.0
optuna>=3.5
kagglehub>=0.2
ipykernel
jupyter
jupytext
pytest
imbalanced-learn>=0.12
# Phase 5/6 (DL track) — install separately on the 4090 server with the
# right CUDA build:  pip install torch --index-url https://download.pytorch.org/whl/cu124
torch>=2.1
